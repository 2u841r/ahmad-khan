[build]
  # Build command
  command = "pnpm run build"
  
  # Publish directory (where VitePress outputs the built site)
  publish = ".vitepress/dist"

# Enable clean URLs (removes .html extension)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Set Node version (required for VitePress 2.0 alpha)
[build.environment]
  NODE_VERSION = "20"

